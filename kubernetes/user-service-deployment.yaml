apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-deployment
  labels:
    app: user-service
spec:
  replicas: 3  # Number of replicas for high availability
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: <your-docker-registry>/user-service:latest  # Replace with your image name
        ports:
        - containerPort: 8080  # Default user-service port
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"  # Optional: Set Spring profiles
        - name: EUREKA_SERVER_URL
          value: "http://eureka-server:8761/eureka/"  # Replace with Eureka server URL if necessary
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
